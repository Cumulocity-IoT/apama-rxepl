package com.industry.rx_epl;

using com.apama.exceptions.Exception;

event Subject {		
	static action create() returns ISubject {
		Observable o := Observable._create();
		return BaseSubject.create(o.asIObservable(), o.setOnConnection);
	}
}