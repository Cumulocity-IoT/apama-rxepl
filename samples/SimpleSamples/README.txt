The .mon files in the folder are each designed to be copied into a new SoftwareAG Designer project

The .rxblocks files can be opened with the RxEPL Builder

Prerequisites:
# Install Apama version 10.1 (patch 5) or higher
# Install RxEPL
# Install Lambdas for Apama (A separate project frequently used with RxEPL)

